name: pak_doc_ai
description: A clinical decision-support mobile MVP.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_spinkit: ^5.2.0
  google_fonts: ^6.1.0
  image_picker: ^1.0.4
  nadra_verisys_flutter: ^0.1.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
